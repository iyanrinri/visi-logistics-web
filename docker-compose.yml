version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: visi-logistics-web:latest
    ports:
      - "3130:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
